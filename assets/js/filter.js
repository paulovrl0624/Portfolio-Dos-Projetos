import{projects}from'./projects-data.js';import{renderProjects}from'./projects-render.js';function uniqueTags(){const s=new Set();projects.forEach(p=>p.tags.forEach(t=>s.add(t)));return[...s]}function buildFilterButtons(el){const tags=uniqueTags();el.innerHTML=['Todos',...tags].map(t=>`<button class="project-filter-btn" data-tag="${t}">${t}</button>`).join('');}function applyFilter(tag){const cards=[...document.querySelectorAll('.project-card')];cards.forEach(c=>{const tags=c.dataset.tags.split(' ');if(tag==='Todos'||tags.includes(tag)){c.style.display='';}else{c.style.display='none';}})}export function initFilters(){const el=document.getElementById('project-filters');if(!el)return;buildFilterButtons(el);renderProjects('projects-grid');const btns=[...el.querySelectorAll('button')];btns.forEach(b=>b.addEventListener('click',()=>{btns.forEach(x=>x.classList.remove('active'));b.classList.add('active');applyFilter(b.dataset.tag)}));if(btns[0])btns[0].classList.add('active')}
