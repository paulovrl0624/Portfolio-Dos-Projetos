import{projects}from'./projects-data.js';function card(p){return`<article class="project-card" data-tags="${p.tags.join(' ')}"><img src="${p.img}" alt="Imagem do projeto ${p.title}"><div class="project-card-body"><h3>${p.title}</h3><p>${p.desc}</p><div class="project-card-links"><a class="btn ghost" href="${p.github}" target="_blank" rel="noopener">GitHub</a><a class="btn primary" href="${p.demo}" target="_blank" rel="noopener">Demo</a></div></div></article>`}export function renderProjects(containerId){const el=document.getElementById(containerId);if(!el)return;el.innerHTML=projects.map(card).join('')}export function renderHighlights(containerId,count=3){const el=document.getElementById(containerId);if(!el)return;el.innerHTML=projects.slice(0,count).map(card).join('')}
